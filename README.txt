</CSS Selectors Used:/>
a Element Selectors

Used to style standard HTML tags like:
h1, h2, p, img

b ID Selector

Example: #profile

Used to target a specific hobby list item and give it a unique color.

c Class Selectors

Examples: .profile, .car

Used to style reusable components like text blocks and the profile card layout.

d Group Selector

Example: h1, h2

Used to apply the same style to multiple elements (like making all headings uppercase and colored navy).

e Descendant Selector

Example: .profile a

Used to target <a> tags inside a specific class (profile section), to customize link styles and hover effects.

f Attribute Selector

Example: li[draggable="true"]

Targets specific list items based on their attributes — used here to highlight a draggable hobby.

g Hover Selector

Example: .profile a:hover

Adds interactivity by changing link colors on hover (e.g., email and LinkedIn turn crimson).

</Styling Techniques Used:/>
a box-shadow: Adds depth to the profile card.

b border-radius: Creates rounded corners.

c margin + padding: Manages spacing inside and around the card.

d text-align, font-family, color: Controls text presentation.

e transition: Smooth hover animations.